{
  "name": "bakfon-erp-functions",
  "description": "Firebase Cloud Functions for Bakfon ERP",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "deploy:issueAuthToken": "cd .. && firebase deploy --only functions:issueAuthToken"
  },
  "engines": { "node": "20" },
  "type": "module",
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "node-fetch": "^3.3.2"
  }
}
